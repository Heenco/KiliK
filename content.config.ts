export default {
  // Configure markdown
  markdown: {
    // Enable rehype plugins for better markdown parsing
    rehypePlugins: [],
    remarkPlugins: []
  },
  // Configure how the content is processed
  experimental: {
    clientDB: true
  }
}
